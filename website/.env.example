# Inlined at build time. After changing either value, trigger a Vercel redeploy.
NEXT_PUBLIC_SITE_URL=https://linea.meetbhatt.com

# Optional. HTML-tag verification from Google Search Console — the string
# inside content="...", not the whole meta tag.
# GOOGLE_SITE_VERIFICATION=
